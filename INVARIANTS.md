# Invariants

## Invariants

| ID | Invariant | The Rule | Applies When | Owner | Derivation Rule | Tier | Mechanical Check | Failure Mode | Status | Superseded By |
|-|-|-|-|-|-|-|-|-|-|-|
| INV-ARCH-01 | Providers are mutually independent | Neither concrete provider imports the other; each is a self-contained translation path | Any provider module import | The `[tool.importlinter]` independence contract | Enforced by the boundary map; a new provider registers into `PROVIDERS` and stays sibling-independent | 2 | `lint-imports` contract "Providers are mutually independent" | Cross-provider coupling — a change to one silently breaks the other | active |  |
| INV-ARCH-02 | Proxy dispatches via the abstraction | `proxy.py` never imports a concrete provider; it dispatches through the `Provider` protocol resolved from `PROVIDERS` | Proxy request dispatch | The `[tool.importlinter]` forbidden-import contract | The proxy depends on the abstraction, not the implementation | 2 | `lint-imports` contract "Proxy dispatches via the provider abstraction" | Proxy hardwired to a provider; boundary inversion | active |  |
| INV-DEP-01 | One runtime dependency: httpx[http2] | The runtime imports the Python standard library plus the single declared dependency `httpx[http2]` (the data-plane HTTP/2 transport in `http_client.py`); no other third-party runtime dependency (dev and build tooling excepted) | Any runtime import | `pyproject.toml` `[project] dependencies` | `dependencies = ["httpx[http2]>=0.27"]` is the sole runtime entry; only `[dependency-groups] dev` and `[build-system]` carry other tooling (D-RUNTIME-002, superseding D-RUNTIME-001) | 2 | `[project].dependencies` holds only `httpx[http2]`, plus the dependency audit (pip-audit) | An unreviewed supply-chain surface added beyond the one vetted data-plane dependency | active |  |
| INV-MEDIA-01 | Media payloads never leak into text | Base64 image or document payloads must never appear in placeholder text, warnings, or logs | Media parsing and forwarding | `content.py` (`parse_media_source`) | Placeholders carry the media type and size only, never the payload bytes | 3 | Content unit tests assert the placeholder excludes the payload | A large base64 media payload leaked into logs or model-visible text | active |  |
| INV-SEC-01 | Secrets never logged | No bearer, key, or refresh-token VALUE reaches a log or trace; only the auth mode name (`grok_oauth`) is emitted | Any logging or trace event | `log.py` and each provider's header construction | Redaction is the caller's contract; log helpers accept types, names, ids, and lengths, never secret values | 2 | `test_log_message_never_contains_the_credential`, `test_message_never_contains_the_token_value` | Credential exfiltration through logs (CWE-532) | active |  |
| INV-SEC-02 | Loopback-only bind | The proxy binds `127.0.0.1` by default and never `0.0.0.0` unless the operator sets an explicit host | Proxy startup | `proxy.py`, `__main__.py` | The default host constant is loopback; a wildcard host is only ever an explicit operator argument | 2 | `test_main_honors_explicit_host_and_port` plus the loopback-default unit test | Unauthenticated LAN exposure of the bridge | active |  |
| INV-SEC-03 | Bearer only in the Authorization header | The opaque bearer rides solely in the `Authorization` header, validated as an RFC 7235 token68 before the header is built | Any upstream request | `providers/xai/auth.py` (`_validated_bearer`), `providers/openai/auth.py` (`_validated_bearer`) | The bearer is validated pre-header; control or non-ascii characters are rejected without leaking the value | 2 | `_validated_bearer` unit tests (control-char bearer rejected credential-free) | Header injection or credential placed in a query/body (CWE-20/532) | active |  |
| INV-SEC-04 | Atomic owner-only credential rewrite | Each provider's credential file (`~/.grok/auth.json`, `~/.codex/auth.json`) is rewritten atomically at mode 0600 on refresh, updating only the selected entry and never widening permissions | OIDC token refresh | `providers/xai/auth.py`, `providers/openai/auth.py` | `mkstemp` opens O_EXCL at 0600, the entry is updated, then an atomic rename carries 0600 onto the file | 2 | Refresh unit tests assert mode 0600 and single-entry update | World-readable token or partial-write corruption (CWE-377/732) | active |  |
| INV-SEC-05 | Refresh only to the pinned issuer | A refresh POST goes only to the token endpoint pinned in code; a poisoned issuer or endpoint in the credential file is ignored | OIDC token refresh | `providers/xai/auth.py`, `providers/openai/auth.py` | The issuer host is pinned in code, never taken from the attacker-controllable credential file | 2 | Negative tests: a non-HTTPS or cross-host issuer is refused with no POST | SSRF / refresh-token exfiltration to an attacker host (CWE-918/200) | active |  |
| INV-SEC-06 | Encrypted reasoning stays in memory | Encrypted reasoning blobs live only in the in-memory reasoning cache — never logged, persisted, or returned to the client | Reasoning round-trip | `providers/xai/provider.py`, `providers/openai/provider.py` | The cache is a per-instance dict with no serialization path; fixtures are sanitized to a synthetic sentinel | 2 | In-memory-only regression guard test | Reasoning-content leak (CWE-312) | active |  |
| INV-SSE-01 | Every started stream terminates | Every started Anthropic SSE stream ends in a terminal event, even when the upstream errors mid-stream | Streaming responses | `stream.py` and each provider's stream translation | The provider event taxonomy maps every terminal upstream outcome to a stream terminator | 3 | Stream fixture tests assert a terminal event for each upstream terminal outcome | Client hang on an unterminated stream | active |  |
