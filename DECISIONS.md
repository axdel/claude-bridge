# Decisions

## Registry

| ID | Title | Disposition | Date | Context | Decision | Rationale | Status | Superseded By |
|-|-|-|-|-|-|-|-|-|
| D-API-001 | Target OpenAI Responses API instead of Chat Completions | accepted | 2026-03-20 | initial OpenAI provider | Target OpenAI Responses API instead of Chat Completions. | Responses has richer tool-call semantics, including `call_id` and `id` separation, that better match Anthropic Messages. | active |  |
| D-AUTH-001 | Prefer API-key auth by default, with Codex OAuth as experimental fallback | accepted | 2026-03-20 | initial OpenAI provider | Prefer API-key auth by default, with Codex OAuth as experimental fallback. | Standard API-key mode is the supported path while subscription OAuth remains policy-gray and local-only. | active |  |
| D-BUILD-001 | Make `claude_bridge` editable-installable via a `[build-system]`, not a PYTHONPATH hack | accepted | 2026-07-10 | feature/swap-gemini-for-grok | Add a hatchling `[build-system]` and `[tool.hatch.build.targets.wheel]` targeting `src/claude_bridge`, alongside an empty `[project] dependencies`, so `uv sync` editable-installs the package and venv tooling can import `claude_bridge` from `sys.path`. | The boundary-map gate runs import-linter/grimp, which needs `root_package` importable; the alternative — teaching the protocol's `tool_env` to prepend `PYTHONPATH=src` — was rejected as a per-tool hack the exemplar (claude-protocol itself) avoids by shipping a build backend, and hatchling is build-time only so the stdlib-only runtime (D-RUNTIME-001) is untouched. | active |  |
| D-CACHE-001 | Bound the provider-local reasoning cache with LRU eviction | accepted | 2026-06-08 | feature/claude-code-compatibility | Bound the provider-local reasoning cache with LRU eviction. | A 256-entry oldest-first cache prevents hidden unbounded provider state while preserving tool-loop continuity. | active |  |
| D-CACHE-002 | Give each provider instance a sticky per-session prompt-cache identity | accepted | 2026-08-22 | feature/client-parity | Generate one random UUID4 `prompt_cache_key` per provider instance at construction and send it on every upstream request for that session — as the `prompt_cache_key` body field for OpenAI Responses, and as both `prompt_cache_key` and the `x-grok-conv-id` header for xAI cli-chat-proxy — so all of a session's turns share one cache identity. | The real grok-build/Codex clients hold a stable per-session cache identity so the upstream prompt cache hits across a tool loop's many turns; a fresh key per request (or none) defeats caching and inflates latency and cost. A process-random UUID4 per instance reproduces that behavior without deriving the key from prompt content — the key MUST NOT embed or hash instructions or reasoning and MUST NEVER be logged (INV-SEC-01/INV-SEC-06). Per-instance not per-request makes it sticky for the session and reset when a new bridge starts; it is orthogonal to the per-request `x-grok-req-id`. | active |  |
| D-CONFIG-001 | Centralize runtime env ownership in a minimal stdlib `config.py` | accepted | 2026-06-08 | feature/audit-drift-remediation | Centralize runtime env ownership in a minimal stdlib `config.py`. | A small config owner fixes scattered env reads without adding dependencies or broad settings injection. | active |  |
| D-CONFIG-002 | Validate ANTHROPIC_REAL_URL scheme and userinfo at startup; do not IP-filter | accepted | 2026-08-23 | feature/client-parity | start_proxy validates the passthrough upstream URL at startup via config.validate_upstream_url: require https, or http only for a loopback host (127.0.0.0/8, ::1, localhost — the local-mock test path); reject any other scheme and any embedded userinfo. Do NOT block private/internal https destinations. | ANTHROPIC_REAL_URL originates from the operator's process environment, not remote input, so the SSRF framing is a misconfiguration guard rather than a remote-exploit filter — an attacker who can set it already has local code execution. The high-value guards are cleartext prevention (CWE-319: a typo'd http:// would forward the prompt and x-api-key in the clear) and userinfo rejection (no credential embedded in the URL). A private/internal-IP blocklist was rejected: it buys nothing against the local trust boundary and would break legitimate self-hosted or corporate-gateway https endpoints, which a passthrough proxy must support. | active |  |
| D-CONTENT-001 | Add a shared policy-free `content.py` media-source parser that both providers DERIVE from | accepted | 2026-06-09 | feature/nontext-content-translation | Add a shared policy-free `content.py` media-source parser that both providers DERIVE from. | Anthropic media-source parsing (base64/url/file → media_type/data/url/filename) is one fact; a single leaf parser stops the OpenAI and Gemini providers re-encoding it divergently (Single Owner). Kept strictly policy-free — no encoding, no warnings, no size policy — per the [grok] YAGNI caveat; if it ever needs provider-specific parameters, that is the trigger to inline it back per-provider. | active |  |
| D-CONTEXT-001 | Advertise a 300k context window to Claude Code from both launchers | accepted | 2026-08-20 | feature/model-defaults-300k | Export `CLAUDE_CODE_MAX_CONTEXT_TOKENS=300000` (overridable) in the `claude` subshell of both `claude-codex` and `claude-grok`. | Claude Code sizes its auto-compact threshold from the assumed model context window; the default assumption is smaller than the Codex/Grok backends actually offer. 300k is a deliberate under-estimate of both real windows (GPT-5.6 larger; grok-4.6 500K per D-XAI-009), so Claude Code defers compaction and uses more context per session without risking a real overflow. Set on the launcher (a Claude Code env var) rather than in the bridge, which never reads it. Kept `:-300000` so an operator can still override per run. | active |  |
| D-DEPS-001 | Add Bandit and pip-audit as dev-only audit tools | accepted | 2026-06-08 | feature/audit-drift-remediation | Add Bandit and pip-audit as dev-only audit tools. | Security gates require reproducible local source/dependency checks, and dev-only dependencies preserve the stdlib-only runtime contract. | active |  |
| D-DEPS-002 | Add import-linter as a dev-only architecture gate | accepted | 2026-07-10 | feature/swap-gemini-for-grok | Add `import-linter` to `[dependency-groups] dev` and declare three `[tool.importlinter]` contracts — providers mutually independent, proxy dispatches via the abstraction, leaf utilities never import orchestration — enforcing the module import DAG. | The boundary-map primitive delegates to `lint-imports`, so the DAG documented in BOUNDARY_MAP.md needs a mechanical enforcer; import-linter is the Python standard for this and, like Bandit and pip-audit (D-DEPS-001), is dev-only — the runtime stays zero-dependency. | active |  |
| D-GEMINI-001 | Remove the Gemini provider entirely, preserving only immutable historical references | accepted | 2026-07-10 | feature/swap-gemini-for-grok | Delete `providers/gemini.py` with its tests and fixtures and unregister the provider, leaving Gemini named only in immutable history (CHANGELOG entries and superseded-decision tombstones). | The user requested a complete removal of Gemini alongside the Grok addition, and a residual text-only provider nobody maintains is dead weight; CHANGELOG history and reversed-decision tombstones stay verbatim per the never-delete rule. | active |  |
| D-GOV-001 | Keep decision records in tracked `DECISIONS.md`, not ignored local `CLAUDE.md` | accepted | 2026-06-08 | feature/audit-drift-remediation | Keep decision records in tracked `DECISIONS.md`, not ignored local `CLAUDE.md`. | The registry must be committed and diffable while project-local memory remains ignored and outside branch scope. | active |  |
| D-GOV-002 | Provision the six mandatory architecture-primitive registries on this branch | accepted | 2026-07-10 | feature/swap-gemini-for-grok | Author CANONICAL_GLOSSARY, BOUNDARY_MAP, DERIVATION_MAP, RESOURCE_OWNERSHIP, INVARIANTS, and MEMORY_GOVERNANCE at the repo root, grounded in the shipped code, rather than merging with the primitive gap waived. | The review merge gate reported the six mandatory primitives as blocking STUB; provisioning them here — versus deferring to a separate prerequisite track or merging under a waiver — closes the gap in the same branch that first exercised the gate, and the registries derive from the existing architecture so they carry no invented facts. | active |  |
| D-LOG-001 | Log provider error status and extracted summaries instead of unredacted upstream payloads | accepted | 2026-06-08 | feature/audit-drift-remediation | Log provider error status and extracted summaries instead of unredacted upstream payloads. | Upstream error payloads can include request-derived content or secrets; bounded summaries preserve operator diagnostics without leaking provider data. | active |  |
| D-MEDIA-001 | Bound media input by the whole-request-body cap, not a second per-media hard cap | accepted | 2026-06-09 | feature/nontext-content-translation | Bound media input by the whole-request-body cap, not a second per-media hard cap. | `_MAX_REQUEST_BODY` (config; enforced at `proxy.py:198` before any media handling, rejecting with HTTP 413) already bounds total request size including base64 media; `_OVERSIZED_MEDIA_BYTES` (5 MiB, `proxy.py:223`) drives a diagnostic warning only, and `_approx_decoded_bytes` (`proxy.py:226`) is pure integer arithmetic (`len(data) * 3 // 4`, no base64 decode) so an oversized pasted image inflates neither memory nor the token estimate. A second per-media hard cap would duplicate the body bound and would wrongly reject valid multi-image requests that sit under the body limit — the body cap is the single source of size truth. | active |  |
| D-MODALITY-001 | Declare OpenAI input-content capabilities per auth-mode backend on the instance, keeping the class attribute conservative | accepted | 2026-06-09 | feature/nontext-content-translation | Declare OpenAI input-content capabilities per auth-mode backend on the instance, keeping the class attribute conservative. | api.openai.com and chatgpt.com (Codex) are one provider class with two backends of differing modality support, so a single class-level `capabilities` is the wrong runtime granularity. `__init__` sets `self.capabilities` per `auth_mode`: api_key → text/image/document + tool-output arrays True (documented public Responses support); codex_oauth → text/image/document + tool-output arrays **False**. The codex defaults come from the live probe (input_image and input_file returned HTTP 200; array-form `function_call_output.output` was NOT probed, so it stays disabled and tool-result media degrades observably until a real tool-loop probe). The class attribute stays the conservative text-only default for Protocol conformance and instance-less callers; instances shadow it, and the proxy + `translate_request` both read the instance. | active |  |
| D-PROVIDER-001 | Declare provider stream and sync-response behavior via `ProviderCapabilities` | accepted | 2026-06-08 | feature/audit-drift-remediation | Declare provider stream and sync-response behavior via `ProviderCapabilities`. | Provider-specific behavior should be explicit in `provider.py` while `proxy.py` continues to own HTTP transport, retries, stats, and errors. | active |  |
| D-PROVIDER-002 | Keep xAI unregistered until a real provider implementation exists | accepted | 2026-06-08 | feature/audit-drift-remediation | Keep xAI unregistered until a real provider implementation exists. | A routable provider whose methods raise `NotImplementedError` breaks the provider contract and failover semantics. | inactive | D-XAI-001 |
| D-PROVIDER-003 | Widen `ProviderCapabilities` (transport-only per D-PROVIDER-001) with additive input-content fields | accepted | 2026-06-09 | feature/nontext-content-translation | Widen `ProviderCapabilities` (transport-only per D-PROVIDER-001) with additive input-content fields. | The mapper needs a declared, testable seam — `input_modalities` and `supports_tool_output_content_parts` — to decide forward-vs-degrade per provider/backend without runtime guessing. Both fields default to the conservative pre-feature behavior (text-only, string tool output) so every existing provider and the sibling `token_count_multiplier` branch construct unchanged. Rejected a separate content-capability dataclass: for a 3-provider world a second value object is more surface than the one widened docstring costs (KISS). | active |  |
| D-REASON-001 | Stateful reasoning continuity qualifies the translation purity rule | accepted | 2026-06-08 | feature/claude-code-compatibility | Stateful reasoning continuity qualifies the translation purity rule. | GPT-5.5 with `store:false` requires clients to echo prior encrypted reasoning for multi-turn tool use, while the pure translation functions remain side-effect-free. | active |  |
| D-RUNTIME-001 | Keep runtime stdlib-only with zero runtime dependencies | accepted | 2026-03-20 | initial project conventions | Keep runtime stdlib-only with zero runtime dependencies. | Portability and supply-chain risk matter more than convenience helpers for this local bridge. | inactive | D-RUNTIME-002 |
| D-RUNTIME-002 | Adopt httpx[http2] as the one runtime dependency for the data-plane transport | accepted | 2026-08-22 | feature/client-parity | Take a single `[project]` runtime dependency — `httpx[http2]>=0.27` — for the DATA-plane transport (HTTP/2 with split connect/stream-idle timeouts, a shared event-loop `AsyncClient`, one pre-body retry), owned by the new `http_client.py` leaf. Token/OIDC refresh stays on stdlib `urllib` in each provider's auth module. Dev and build tooling stay dev-only. | A single stdlib `urllib` socket timeout fired on every `recv` and killed long-thinking grok-4.6 streams at ~120s; urllib also cannot do HTTP/2 multiplexing or per-phase timeouts, which the real grok-build/Codex clients rely on. httpx buys `Timeout(connect, read=idle, write=idle, pool)` so a healthy stream survives long thinking while a dead connect fails fast, plus HTTP/2 keep-alive across turns. Scoped to the data plane only — the SSRF-issuer-pinned refresh POST (D-XAI-007) stays off-event-loop on urllib — so the supply-chain surface is one reviewed, pinned dependency. Supersedes D-RUNTIME-001's zero-runtime-dependency stance for the data plane. | active |  |
| D-RUNTIME-003 | Launchers run the bridge through the project venv interpreter, provisioned by uv sync | accepted | 2026-08-23 | feature/client-parity | The claude-grok and claude-codex launchers run the bridge through the project venv's interpreter (${BRIDGE_DIR}/.venv/bin/python), provisioned once by `uv sync`, and abort at startup with a "run uv sync" message if that venv is absent. Every claude_bridge invocation in the launcher — the bridge process, the readiness socket probe, and the banner version/model reads — goes through that one interpreter; the system python3 is no longer used. | After D-RUNTIME-002 made `httpx[http2]` the one runtime dependency, the previous `python3 -P -m claude_bridge` under the system interpreter failed at startup with ModuleNotFoundError: httpx — the documented run path (symlink launchers, no install step) was broken end to end. Rejected `uv run`: the launchers background the bridge and kill it from an EXIT/INT/TERM trap, but under `uv run` the captured `$!` is uv's PID, not the bridge's, so signal delivery to the Python child is not guaranteed. Rejected a global `pip install httpx`: it pollutes the system Python and diverges from the project's uv-native workflow (uv run pytest, editable install via uv sync, D-BUILD-001). The direct venv interpreter keeps signal handling identical to the pre-httpx launcher and adds one honest `uv sync` step. | active |  |
| D-SCOPE-001 | Drop Gemini media translation from this feature and leave `gemini.py` text-only as-is | accepted | 2026-06-09 | feature/nontext-content-translation | Drop Gemini media translation from this feature and leave `gemini.py` text-only as-is. | The user narrowed scope mid-branch — Gemini is no longer under active development for this bridge (no media support added; Grok will receive the canonical media treatment later), so building Gemini media support is unrequested scope; removing `gemini.py` was also not requested, so its existing text/tool path stays unchanged and media degrades observably there until Gemini is removed or revived. | inactive | D-GEMINI-001 |
| D-SEC-001 | Relay bounded provider error diagnostics to the requesting client (visible errors) | accepted | 2026-08-23 | feature/client-parity | Relay upstream provider error text to the requesting client and log an operator-safe summary — the visible-errors goal — but bound both the client-relayed message and the logged summary; never emit unbounded provider-authored text. | The provider is a first-party issuer-pinned HTTPS endpoint (D-XAI-007); its error text reaches the request's own owner (the user's CLI), not a third party, so CWE-209 is calibrated down. INV-SEC-01/06 govern our OWN by-construction secret handling; a provider echoing request content back is a distinct provider-originated vector. The client message was unbounded — an echoed reasoning blob (INV-SEC-06) or prompt-cache key (INV-SEC-01) would relay whole — now capped to _PROVIDER_MESSAGE_LIMIT, matching the log side's safe_log_excerpt bound-plus-control-strip. Rejected a fixed generic client message: it kills the feature's diagnosability. Residual accepted: a bounded prefix of an echoed value can still reach local operator logs and the requesting client under a compromised first-party provider. | active |  |
| D-SEC-002 | Bound all inbound request size (body + headers); accept provider-response buffering under the trust model | accepted | 2026-08-23 | feature/client-parity | Enforce ceilings on every attacker-influenceable inbound request dimension: the Content-Length body cap (_MAX_REQUEST_BODY, 413) and the header block (_MAX_REQUEST_HEADERS line count plus _MAX_REQUEST_HEADER_BYTES aggregate, 413 via _RequestTooLarge). Do NOT add a size ceiling to buffered provider/upstream RESPONSE bodies on this branch. | The bridge binds 127.0.0.1 loopback only, so inbound clients are local processes already running as the user; a hostile one can read the provider auth.json directly, making a header-flood DoS strictly weaker than what it can already do. The caps still close it for a merely buggy local client at negligible cost (CWE-400). The response-buffering residual is accepted: the provider is an operator-chosen, issuer-pinned HTTPS endpoint (D-XAI-007) already receiving every prompt and credential, so an unbounded response requires already BEING that trusted provider, where buffering DoS adds nothing. Streaming-count-and-abort on the buffered POST path was rejected as disproportionate here. Revisit trigger: any non-loopback bind or untrusted-provider/multi-tenant mode makes response-body ceilings load-bearing. | active |  |
| D-SRVTOOL-001 | Treat unsupported server-tool blocks as redacted unsupported content | accepted | 2026-06-08 | feature/claude-code-compatibility | Treat unsupported server-tool blocks as redacted unsupported content. | Unknown server-tool blocks have no proven target-provider equivalent, so safe degradation beats speculative translation. | active |  |
| D-SSE-001 | Cap malformed SSE buffers instead of rewriting normal buffering | accepted | 2026-06-08 | feature/claude-code-compatibility | Cap malformed SSE buffers instead of rewriting normal buffering. | A 4 MiB cap bounds malformed streams while well-formed SSE drains quickly, making a bytearray rewrite unnecessary. | active |  |
| D-STREAM-001 | Map the full OpenAI Responses terminal-event taxonomy to an Anthropic stream terminator | accepted | 2026-06-11 | bugfix/stream-terminal-events (silent mid-work halt on GPT-5.5 incomplete/failed turns) | Map the full OpenAI Responses terminal-event taxonomy to an Anthropic stream terminator. | The Responses streaming API ends a turn with four DISTINCT top-level event types — `response.completed`, `response.incomplete` (max_output_tokens/content_filter), `response.failed` (server_error), or a bare top-level `error` — never a `completed` event with a nested non-completed status. The original dispatcher routed only `response.completed` to the terminal handler and dropped the rest to `return []`, so a GPT-5.5 turn ending incomplete (common under hardcoded xhigh) or failed produced no `message_stop` and Claude Code halted mid-work silently (HTTP 200). Fix: route `response.incomplete` to the terminal handler (maps to max_tokens/end_turn + refusal on content_filter); translate `response.failed` and top-level `error` to an Anthropic `error` event carrying the verbatim length-bounded reason. Also captures reasoning continuity (D-REASON-001) on incomplete, not only completed. | active |  |
| D-STREAM-002 | Enforce a termination invariant in `translate_stream` independent of the upstream event taxonomy | accepted | 2026-06-11 | bugfix/stream-terminal-events (silent mid-work halt on GPT-5.5 incomplete/failed turns) | Enforce a termination invariant in `translate_stream` independent of the upstream event taxonomy. | Mapping each known terminal event (D-STREAM-001) fixes the observed cases but not the class of bug: any future unhandled terminal, or a dropped upstream connection, would again leave a started turn without a closer. `translate_stream` tracks whether a `message_start` was emitted and whether a terminator (`message_stop`/`error`) followed; if a started stream ends without one, it synthesizes `message_delta` + `message_stop` (`stop_reason = tool_use` when tool calls were already emitted so Claude Code runs them, else `end_turn` — never `max_tokens`, which would trigger an auto-compact retry loop). The invariant fires ONLY when a `message_start` was sent (a stream that produced no output stays empty) and never when a real terminator already arrived (no duplicate terminator). This makes "started stream with no closer" structurally impossible regardless of what the upstream sends. | active |  |
| D-STREAM-003 | Streaming auto-mode has no circuit-breaker failover | accepted | 2026-08-23 | feature/client-parity | Streaming auto-mode requests (no provider set, stream requested) pass through to Anthropic without consulting the router. The proxy logs a WARNING when the breaker is OPEN but does not fail over; the sync auto-route path keeps its failover. | A provider switch mid-stream is impossible once response bytes are committed to the client, and pre-stream streaming failover is outside client-parity scope (Requirements lens PASS). Rejected: (a) failing over before the first byte — adds a second provider round-trip and a divergent streaming path for a case the scope excludes; (b) refusing the stream when OPEN — worse than serving it, since Anthropic may still be reachable. The WARNING makes the unprotected stream visible without changing behavior. | active |  |
| D-STRUCT-001 | Split the provider god-files into cohesive sub-packages | accepted | 2026-08-21 | feature/client-parity | Decompose the flat `providers/xai.py` and `providers/openai.py` god-files into sub-packages — each with `provider.py` (entry), `auth.py` (credential/refresh), `translate.py` (request/response), and `stream.py` (stream translation) — behavior-preserving, every file under the god-file line ceiling. | Both flat files exceeded FILE_LINE_CEILING and the god-file gate blocks a merge over it; the client-parity changes (httpx transport wiring, split timeouts, sticky cache identity) would only push them further over. Splitting by cohesive concern keeps each file readable and gate-clean with no behavior change, proven by the unchanged passing test suite. Preserves D-XAI-002's one-translation-path-per-provider isolation (no cross-provider imports, INV-ARCH-01) at package granularity — the sub-packages remain mutually independent siblings. | active |  |
| D-STRUCT-002 | Extract a shared http_client.py data-plane transport leaf owned by the proxy | accepted | 2026-08-22 | feature/client-parity | Put the httpx HTTP/2 mechanics — shared event-loop `AsyncClient`, split-phase `Timeout`, one pre-body retry, buffered-POST and streaming-open helpers — in a single `http_client.py` leaf imported only by `proxy.py` and `proxy_streaming.py`. Providers stay transport-free (translation only); the proxy drives the upstream call on a provider's behalf via `post_provider`/`open_stream`. | Transport mechanics are provider-agnostic infrastructure the proxy already owns (INV-ARCH-02), not translation, so they sit in one leaf rather than duplicated per provider or leaked into provider modules. This keeps providers doing pure translation (BOUNDARY_MAP: providers perform no HTTP I/O) and D-XAI-002's per-provider translation duplication untouched — sharing transport creates no cross-provider coupling because providers import neither http_client nor each other (INV-ARCH-01 holds). A transport fix (timeout split, retry, keep-alive) then lands once. | active |  |
| D-THINK-001 | Preserve native reasoning formats rather than cross-mapping semantics | accepted | 2026-03-20 | reasoning-mode design | Preserve native reasoning formats rather than cross-mapping semantics. | Anthropic thinking and provider reasoning are not equivalent, so opaque passthrough or dropping is safer than pretending they translate. | active |  |
| D-TOKEN-001 | Estimate media input tokens with a flat per-modality budget (image 1200, document 3000) computed inline in `proxy.py` | accepted | 2026-06-09 | feature/nontext-content-translation | Estimate media input tokens with a flat per-modality budget (image 1200, document 3000) computed inline in `proxy.py`, independent of base64 payload size. | `estimate_input_tokens` previously counted a base64 payload as text bytes (a ~300 KiB pasted image read as ~114k phantom tokens), corrupting Claude Code's auto-compact signal at `/v1/messages/count_tokens`; model media cost is dominated by fixed per-item processing (vision tiles, per-page render/extract) rather than base64 length, so a flat budget tracks reality better than byte-counting and keeps text-only estimates byte-identical (int media tokens add after rounding). Kept inline in `proxy.py` rather than in the policy-free `content.py` leaf because pre-routing token estimation is a proxy-owned concern with no provider, so `content.py` stays parse-only per D-CONTENT-001. | active |  |
| D-TRACE-001 | Use open-append-close JSONL trace writes for compatibility diagnostics | accepted | 2026-06-08 | feature/claude-code-compatibility | Use open-append-close JSONL trace writes for compatibility diagnostics. | The opt-in fail-open trace is simpler and more rotation-friendly without a long-lived handle. | active |  |
| D-TRANSPORT-001 | Ship Codex/OpenAI over HTTP+key; defer a production Responses WebSocket stack | accepted | 2026-08-22 | feature/client-parity | Keep HTTP + API-key/OAuth as the Codex/OpenAI transport for this branch; do not build a production Responses WebSocket transport. Record the WS handshake probe as evidence only. | A live probe of the private Responses WebSocket endpoint returned `HTTP/1.1 101 Switching Protocols` with a valid `sec-websocket-accept` against chatgpt.com using the OAuth bearer and `OpenAI-Beta: responses_websockets=2026-02-06`, so the handshake is reachable — but a 101 proves only the handshake, not a working Router or failover. A production WS transport needs JSON-frame demux, ~60-minute reconnect, and `store=false` reasoning replay: a dedicated design beyond client parity that would also add a websockets runtime dependency. HTTP+key is proven, sufficient, and already shipped, so WS stays a documented follow-up. | active |  |
| D-USAGE-001 | Report OpenAI usage as flat Anthropic totals | accepted | 2026-06-08 | feature/claude-code-compatibility | Report OpenAI usage as flat Anthropic totals. | OpenAI `input_tokens` and `output_tokens` already include cached and reasoning details, so splitting them into Anthropic cache fields would double-count. | active |  |
| D-USAGE-002 | Do not add a bridge-side usage floor for Claude Code autocompact without provider-response evidence | accepted | 2026-06-08 | feature/audit-drift-remediation | Do not add a bridge-side usage floor for Claude Code autocompact without provider-response evidence. | Claude Code `/context` category boxes are diagnostic estimates, while auto-compact is driven by API usage plus local deltas; changing reported usage from local estimates could over-compact or double-count. | inactive | D-USAGE-003 |
| D-USAGE-003 | Apply a provider token-count multiplier and set OpenAI/GPT to 1.2 | accepted | 2026-06-09 | bugfix/token-count-multiplier | Apply a provider token-count multiplier and set OpenAI/GPT to 1.2. | The multiplier is an explicit compatibility knob for empirical Claude Code auto-compact tuning, kept at the provider capability boundary instead of re-estimating prompt structure in the proxy. | inactive | D-USAGE-004 |
| D-USAGE-004 | Lower the OpenAI/GPT token-count multiplier from 1.2 to 1.1 | accepted | 2026-08-20 | feature/model-defaults-300k | Set `GPT_TOKEN_COUNT_MULTIPLIER` (the OpenAI provider's `token_count_multiplier` capability) to 1.1, down from 1.2. | The multiplier inflates reported input/output token counts so Claude Code's auto-compact fires earlier; 1.2 was the initial empirical value (D-USAGE-003). Lowering to 1.1 reduces the inflation so compaction is deferred and more real context is used per session, consistent with the 300k window advertised in D-CONTEXT-001. Still a single knob at the provider capability boundary; xAI's identity 1.0 (D-XAI-005) is unaffected. Mild step (monitored via live usage) rather than removing the knob, since some inflation still hedges the Claude-vs-GPT tokenizer gap. | active |  |
| D-XAI-001 | Back the xAI provider with the subscription-metered cli-chat-proxy, not api.x.ai | accepted | 2026-07-10 | feature/swap-gemini-for-grok | Route the xAI provider to `https://cli-chat-proxy.grok.com/v1/responses`, authenticated with the grok CLI subscription bearer (OIDC refresh) plus the `x-grok-client-version` / `x-grok-client-identifier` gate headers. | The subscription proxy meters against the existing Grok subscription and speaks the Responses wire verbatim, so it mirrors the Codex OAuth model with no API key; rejected `api.x.ai/v1/responses` because it bills as a separate metered API per xAI docs. | active |  |
| D-XAI-002 | Implement `xai.py` as a self-contained duplicate of the Responses translation (time-bounded debt) | accepted | 2026-07-10 | feature/swap-gemini-for-grok | Duplicate the OpenAI Responses request/response/stream translation into a self-contained `xai.py` with no cross-provider imports, rather than extracting a shared Responses core. | The user refused to re-touch the proven `openai.py` path, and the one-file-per-provider boundary preserves failure isolation; accepted as time-bounded duplication debt whose extraction trigger is the 3rd Responses-family provider (Rule of Three). | active |  |
| D-XAI-003 | Resolve the grok credential from `~/.grok/auth.json` with re-read-before-write atomic replacement | accepted | 2026-07-10 | feature/swap-gemini-for-grok | Read the subscription bearer/refresh from `~/.grok/auth.json` (test-overridable `auth_path`), refresh via OIDC on expiry, and persist a rotated token by re-reading then atomically replacing the file at owner-only (0600) perms. | The grok CLI may refresh the same file concurrently, so re-read-before-write plus atomic rename plus 0600 perms reduce (do not eliminate) lost-update risk without a long-lived handle; no credential value is ever logged or placed in error messages. | active |  |
| D-XAI-004 | Key encrypted-reasoning continuity by the exact upstream `call_id`, characterized from live fixtures | accepted | 2026-07-10 | feature/swap-gemini-for-grok | Re-inject each turn's encrypted reasoning item keyed by the VERBATIM upstream `call_id` (no `_to_openai_id` rewrite), holding the blobs in memory only. | Captured cli-chat-proxy fixtures show the proxy echoes the client's exact `call_id`, so rewriting it would break multi-turn tool continuity; the encrypted reasoning is opaque and must never be persisted, logged, or returned to Claude Code (qualifies the translation-purity rule per D-REASON-001). | active |  |
| D-XAI-005 | Report xAI usage as flat Anthropic totals with an identity token-count multiplier | accepted | 2026-07-10 | feature/swap-gemini-for-grok | Flat-map xAI Responses `input_tokens`/`output_tokens` to Anthropic totals and set `token_count_multiplier = 1.0` (identity). | The subscription proxy is metered by xAI and not re-tokenized by the bridge, so no OpenAI-compat 1.2 scaling applies; Grok's 1M context does not change Claude Code's local auto-compact math, which keys off the reported totals (parallels D-USAGE-001). | active |  |
| D-XAI-006 | Source the `x-grok-client-version` gate dynamically from the installed CLI, with a pinned floor and env override | accepted | 2026-07-10 | feature/swap-gemini-for-grok | Resolve `x-grok-client-version` at runtime from the installed grok CLI (glob), fall back to a pinned floor `0.1.202`, and allow an `XAI_CLIENT_VERSION` env override. | cli-chat-proxy rejects requests below its required client version and may raise the floor over time; sourcing from the auto-updating CLI tracks the gate, the pinned floor still clears today's gate when the CLI is absent, and the env override is the manual escape hatch — an accepted maintenance dependency. | active |  |
| D-XAI-007 | Pin the xAI OIDC issuer and refuse cross-host or non-HTTPS token refresh | accepted | 2026-07-10 | feature/swap-gemini-for-grok | Pin the refresh issuer to the hardcoded HTTPS constant `https://auth.x.ai`, ignore any `oidc_issuer` field carried in `~/.grok/auth.json`, and refuse to POST the refresh_token to a non-HTTPS scheme or a cross-host endpoint. | A poisoned `auth.json` could otherwise redirect the refresh_token and client_id to an attacker-controlled or internal host (SSRF / credential exfiltration, CWE-918/200); the issuer is a fixed xAI endpoint, so trusting a file-provided `oidc_issuer` buys nothing and only opens the exfil path — rejected honoring it. | active |  |
| D-XAI-008 | Default xAI model = grok-build (rolling latest-coding alias), not pinned grok-4.20 | accepted | 2026-07-13 | 2026-07-13 (hotfix) | Set the config-owned `DEFAULT_XAI_MODEL` to `grok-build` and derive the `claude-grok` launcher banner model from `config.xai_model()` instead of hardcoding a model literal. | `grok-build` is xAI's rolling alias for the latest Grok coding model (currently Grok 4.3, 512K context) and the grok CLI's own default — verified against `~/.grok/models_cache.json` and the `cli-chat-proxy.grok.com` `/v1/models` origin — whereas the prior `grok-4.20` was a pinned, now-stale version (resolved upstream to `grok-4.20-0309-reasoning`), so pinning a version number was rejected for the alias that rolls forward with the subscription automatically. | inactive | D-XAI-009 |
| D-XAI-009 | Pin default xAI model to grok-4.6 (explicit request), superseding the grok-build alias | accepted | 2026-08-20 | feature/model-defaults-300k | Set the config-owned `DEFAULT_XAI_MODEL` to the pinned `grok-4.6` instead of the rolling `grok-build` alias. The banner still derives from `config.xai_model()`, so it tracks the pin; `XAI_MODEL` still overrides per run. | Requested explicitly to run `claude-grok` on Grok 4.6, newer than `grok-build`'s current Grok 4.3 target. `grok-4.6` was verified accepted by `cli-chat-proxy.grok.com`'s `/v1/responses` endpoint (the sibling `grok-4.6-build` 404s, and `grok models` lists only `grok-build`, but the backend accepts specific version ids too). Tradeoff vs D-XAI-008: pinning forfeits the alias's automatic roll-forward, so a future upstream deprecation (as befell `grok-4.20`) surfaces as a model error until the pin is bumped — a one-line, `XAI_MODEL`-overridable revert to `grok-build`. | active |  |
| D-XAI-010 | Send a model-gated reasoning.effort=low for grok-4.6+, omitting it for pre-4.6 | accepted | 2026-08-22 | feature/client-parity | Add a config-owned, `XAI_REASONING_EFFORT`-overridable `reasoning.effort` (default `low`) to xAI Responses requests, emitted only when the resolved model accepts it — sent to grok-4.6+ and omitted for pre-4.6 models. Gate on "provably old" (`_model_accepts_reasoning_effort`), not an allowlist of new ids. | Live probes (U-EFFORT) showed grok-4.6 accepts `reasoning.effort` omit/low/medium/high all at HTTP 200, while grok-4.20 and earlier 400 on the field (field_effort_low.json) — so the field is model-gated to avoid breaking pre-4.6. `low` is the default because high/xhigh thinking lengthens the stream toward the 120s wall and slows interactive coding turns, while low keeps grok-4.6 responsive and still reasoning. Gating on provably-old rather than provably-new keeps effort working on future models by default. Orthogonal to reasoning `include` (continuity, D-XAI-004): include controls whether the encrypted blob returns, effort controls how much the model thinks. | active |  |

## Invalidations

| Decision | Target | Note | Status | Superseded By |
|-|-|-|-|-|

## Amendments

| Amending | Amended | Note | Status | Superseded By |
|-|-|-|-|-|
